Optional build resources for packaging live here.

Recommended next asset:
- `icon.ico` for the Windows executable and installer

If `icon.ico` is not provided yet, Electron Builder will fall back to the default Electron icon.
